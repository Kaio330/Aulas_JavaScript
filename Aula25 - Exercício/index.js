function MaiorNumero(numero1, numero2) {

    return numero1 > numero2 ? numero1 : numero2;
}

const Max = (numero1, numero2) => numero1 > numero2 ? numero1 : numero2;

// Outra forma de fazer

console.log(Max(10, 5));